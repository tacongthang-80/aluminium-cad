{
  "name": "aluminium-cad-geometry",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "dev": "vite",
    "build": "tsc && vite build"
  },
  "devDependencies": {
    "typescript": "~5.9.0",
    "vite": "^7.0.0",
    "vitest": "^3.2.0",
    "@vitest/coverage-v8": "^3.2.0"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}